{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "GDB Launch",
            "MIMode": "gdb",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "build",

            "cwd": "${workspaceFolder}/sort",
            "program": "${workspaceFolder}/sort/quick_bubble_select.out",
            "args": ["arg1", "arg2", "arg3"]
        }
    ]
}